;; name project rk3dxs
;; name program rk
;; name extension newlisp
;; name describer dxs
;; name active x the twitter
;; name data 3
;; name unit 3

;; active url x the twitter to actived procedural agreements logical local brazil 
;; agreement business brazil web site rede social and community x the twitter
(setf '(url-port twitter) 80)

;; apply connection active x the twitter actived local brazil 
(setf '(url-http twitter) 80)

;; connected the x the twitter in local brazil 
(set 'actived-url-port (apply append url-port))
(set 'actived-url-http (apply append url-http))

;; addition url x the twitter message actived yo
(apply add x-twitter)

